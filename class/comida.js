let comidaX;
let comidaY;

class Comida{
    constructor(posicaoX, posicaoY, tamanho, tamanho) {
        rect(posicaoX, posicaoY, tamanho, tamanho);
    }

    atualizar(){
        comidaX = floor(random(0, 780))
        comidaY = floor(random(0, 480))       
    }















}